{
  "name": "portfolio-market-proxy",
  "private": true,
  "version": "5.1.0",
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node smoke-test.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  },
  "engines": {
    "node": ">=22.12"
  }
}
